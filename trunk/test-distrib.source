mvn clean install -o -pl distrib -am -Dmaven.test.skip && mkdir distrib/target/noweco && pushd distrib/target/noweco && unzip ../noweco.distrib-*-SNAPSHOT-distribution.zip && chmod +x noweco.sh > /dev/null